# Celery uses late acknowledgements for crash-safe transcoding. Keep RabbitMQ's
# delivery acknowledgement deadline above the Celery hard limit plus margin.
# The value is validated independently by every transcoder process at startup.
consumer_timeout = $(RABBITMQ_CONSUMER_TIMEOUT_MS)
